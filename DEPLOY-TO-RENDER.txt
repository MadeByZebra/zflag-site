ZFLAG — Render deployment files

Keep these files together in the root of your GitHub repository:
  index.html
  ZFLAG-Setup-v2.0.23.exe
  render.yaml

Installer SHA-256:
40e9bb0ea2de7523c004e58d2ff9dea3cbc04a4fe46bfb4ecf05cf6d9dd7a7a6

Installer size (bytes):
13059660

Every visible Download button in index.html is wired directly to:
  ZFLAG-Setup-v2.0.23.exe

The render.yaml also sends Content-Disposition: attachment for the installer,
so browsers download it instead of attempting to display it.

